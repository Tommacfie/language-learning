query GetWords {
  wordMany {
    english
    polish
    englishDescription
    polishDescription
    tags
  }
}
query GetUsers {
  userMany {
    name
  }
}

mutation CreateWord($record: CreateOneWordInput!) {
  wordCreateOne(record: $record) {
    recordId
  }
}
