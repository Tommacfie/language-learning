query GetWords {
  wordMany {
    english
    polish
    englishDescription
    polishDescription
  }
}
query GetUsers {
  userMany {
    name
  }
}

mutation CreateWord($record: CreateOneWordInput!) {
  wordCreateOne(record: $record) {
    recordId
  }
}
