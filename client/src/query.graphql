query GetWords {
  wordMany {
    english
    polish
    englishDescription
    polishDescription
    tags
    wordGender
    wordType
  }
}
query GetUsers {
  userMany {
    name
  }
}

mutation CreateWord($record: CreateOneWordInput!) {
  wordCreateOne(record: $record) {
    recordId
  }
}
